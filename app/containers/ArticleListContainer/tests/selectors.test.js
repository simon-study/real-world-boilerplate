// import { fromJS } from 'immutable';
// import { selectArticleListContainerDomain } from '../selectors';

describe('selectArticleListContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
